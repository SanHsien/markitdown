# MarkItDown 核心依賴（純文字、HTML 與基礎轉換）
beautifulsoup4>=4.11.0  # freshness-hold: upstream compatibility floor
requests>=2.28.0  # freshness-hold: upstream compatibility floor
markdownify>=0.11.6  # freshness-hold: upstream compatibility floor
magika~=0.6.1  # freshness-hold: upstream pinned to 0.6.x line
charset-normalizer>=3.0.0  # freshness-hold: upstream compatibility floor
defusedxml>=0.7.1  # freshness-hold: upstream compatibility floor
