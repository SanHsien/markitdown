-r requirements.txt

# Office 辦公文件（PowerPoint、Word、Excel）
python-pptx>=1.0.0  # freshness-hold: upstream compatibility floor
mammoth~=1.11.0  # freshness-hold: upstream pinned to 1.11.x line
lxml>=5.0.0  # freshness-hold: upstream compatibility floor
pandas>=2.0.0  # freshness-hold: upstream compatibility floor
openpyxl>=3.1.0  # freshness-hold: upstream compatibility floor
xlrd>=2.0.0  # freshness-hold: upstream compatibility floor

# PDF 與 Outlook
pdfminer.six>=20251230  # freshness-hold: upstream compatibility floor
pdfplumber>=0.11.9  # freshness-hold: upstream compatibility floor
olefile>=0.46  # freshness-hold: upstream compatibility floor

# 音訊轉譯與 YouTube
pydub>=0.25.1  # freshness-hold: upstream compatibility floor
SpeechRecognition>=3.10.0  # freshness-hold: upstream compatibility floor
youtube-transcript-api>=1.0.0  # freshness-hold: Python 3.14 requires >=1.2.3; upstream 1.0.x is locked to <3.14

# 雲端 Azure AI Document Intelligence & Content Understanding
azure-ai-documentintelligence>=1.0.0  # freshness-hold: upstream compatibility floor
azure-ai-contentunderstanding>=1.2.0b1  # freshness-hold: upstream compatibility floor
azure-identity>=1.15.0  # freshness-hold: upstream compatibility floor
